# LoRA SFT training stack for finetune/. Deliberately excludes bitsandbytes:
# the target models (LFM 350M / Hammer 1.5B) are small enough to LoRA-tune in
# fp32/bf16 without 4-bit quantization, and bitsandbytes' Metal/MPS kernels are
# still an unaccelerated fallback (see finetune/README.md for details). Revisit
# if a larger base model is ever considered.
#
# Installed into finetune/.venv with:
#   python3 -m venv finetune/.venv
#   finetune/.venv/bin/pip install -r finetune/requirements.txt

torch==2.13.0
transformers==5.14.1
peft==0.19.1
accelerate==1.14.0
datasets==5.0.0
matplotlib>=3.8.0
